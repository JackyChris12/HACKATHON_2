<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My Livestock Logs</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f6f9;
      color: #333;
      padding: 2rem;
    }

    h2 {
      color: #2c3e50;
      margin-bottom: 1.5rem;
    }

    ul {
      list-style-type: none;
      padding: 0;
    }

    li {
      background-color: #fff;
      margin-bottom: 1.5rem;
      padding: 1rem;
      border-left: 5px solid #2ecc71;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
      border-radius: 6px;
    }

    .log-title {
      font-weight: bold;
      font-size: 1.1rem;
      color: #34495e;
    }

    .log-detail {
      margin-top: 0.5rem;
      line-height: 1.4;
    }
  </style>
</head>
<body>
    <h2>My Livestock Logs</h2>

<% if (logs.length > 0) { %>
  <ul>
    <% logs.forEach(log => { %>
      <li>
        <strong><%= log.animal_name || "Unknown Animal" %> (<%= log.livestock_type || "Unknown Type" %>)</strong> — Log Date: <%= new Date(log.log_date).toLocaleDateString() %><br />
        Feed: <%= log.feed %><br />
        Symptoms: <%= log.symptoms %><br />
        Production: <%= log.production %><br />
        Notes: <%= log.notes || "None" %>
      </li>
    <% }) %>
  </ul>
<% } else { %>
  <p>No logs available.</p>
<% } %>


</body>
</html>
